# Explicit config for core's generalized record-fields gate (core issue #66).
# Role-unique value only — the checking logic itself now lives in core.
RECORD_FIELDS_REQUIRED="stride-table,mitigation-list,residual-risk-note"

# No terminal-loop_state divergence exists for this role yet (per phase-1
# survey); RECORD_FIELDS_TERMINAL_STATES is intentionally unset here. Add it
# using the same explicit-named-config pattern if a role-specific terminal
# state set is ever needed.
